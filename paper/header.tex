% Packages
\usepackage{amsmath, amssymb, amsthm}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{setspace}
\usepackage{float}
% threeparttable: needed by kableExtra::footnote(); declared here explicitly
% because kableExtra registers it as a chunk side effect, which knitr cache
% hits do not replay
\usepackage{threeparttable}
% caption: supports line breaks inside \caption{}, used for the
% one-line-title-plus-Notes figure caption convention
\usepackage{caption}
\captionsetup{font=small, justification=justified, singlelinecheck=false}

% Theorem environments
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

% Formatting
\setcitestyle{authoryear,round,semicolon}
% Number sections down to subsubsection only; deeper levels (\paragraph) were
% rendering as artifacts like "D.2.0.1" with pandoc --number-sections
\setcounter{secnumdepth}{3}

% Custom commands
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\text{Var}}

% Author block (overrides pandoc's escaped \author at \begin{document})
\AtBeginDocument{\author{John Iselin\thanks{The Budget Lab at Yale} \and Erica Ryan\thanks{Amazon}}}
