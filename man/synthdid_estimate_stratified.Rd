% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stratified.R
\name{synthdid_estimate_stratified}
\alias{synthdid_estimate_stratified}
\title{Stratified weighted SDID estimate.}
\usage{
synthdid_estimate_stratified(
  Y,
  N0,
  T0,
  strata,
  treated.weights = NULL,
  cluster = NULL,
  min.controls = 2,
  drop.infeasible = FALSE,
  ...
)
}
\arguments{
\item{Y}{the observation matrix.}

\item{N0}{the number of control units. Rows 1-N0 of Y correspond to the control units.}

\item{T0}{the number of pre-treatment time steps. Columns 1-T0 of Y correspond to pre-treatment time steps.}

\item{strata}{a vector of length nrow(Y) (controls first, like Y) giving each unit's stratum.
Coerced to factor. Every stratum that contains a treated unit must also contain at
least \code{min.controls} control units, unless drop.infeasible = TRUE.}

\item{treated.weights}{A vector of weights for treated units (length N1 = nrow(Y) - N0).
If NULL, uses uniform weights. Normalized to sum to 1.}

\item{cluster}{An optional vector of length nrow(Y) of cluster IDs (e.g. state FIPS).
Stored for cluster-robust vcov(); also subset into each within-stratum fit.}

\item{min.controls}{Minimum number of same-stratum controls required for a stratum containing
treated units to be estimable. Default 2.}

\item{drop.infeasible}{If TRUE, strata with treated units but fewer than \code{min.controls}
controls (or no treated units after resampling) are dropped and the remaining stratum
shares renormalized, rather than raising an error. Used by the bootstrap, where a
resample can empty one side of a stratum. Default FALSE.}

\item{...}{additional options passed to every within-stratum \link{synthdid_estimate_weighted}
call (e.g. detrend = TRUE).}
}
\value{
The aggregate estimate: a scalar of class 'synthdid_estimate_stratified' with attributes
'strata.fits' (named list of within-stratum synthdid_estimate_weighted objects),
'strata.table' (data.frame: stratum, N1, N0, share, estimate),
'setup' (list: Y, N0, T0, strata), 'treated.weights', 'cluster', and 'opts'
(the extra arguments, re-applied on every vcov() refit).
}
\description{
Partitions units into strata (e.g. county-size bins), runs weighted SDID
within each stratum with the donor pool restricted to same-stratum controls,
and aggregates the within-stratum estimates using the treated-weight share
of each stratum. Because the aggregation weights are the researcher-chosen
treated-unit weights, the aggregate estimates the same weighted estimand as
\link{synthdid_estimate_weighted}; stratification only restricts which controls
each treated unit may be compared to.
}
