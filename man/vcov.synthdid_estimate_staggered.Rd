% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staggered.R
\name{vcov.synthdid_estimate_staggered}
\alias{vcov.synthdid_estimate_staggered}
\title{Variance-covariance matrix for a staggered weighted SDID estimate (bootstrap only).}
\usage{
\method{vcov}{synthdid_estimate_staggered}(
  object,
  method = c("bootstrap"),
  replications = 200,
  cluster = attr(object, "cluster"),
  ...
)
}
\arguments{
\item{object}{a synthdid_estimate_staggered model.}

\item{method}{only "bootstrap" is supported.}

\item{replications}{number of bootstrap replications.}

\item{cluster}{optional cluster IDs; defaults to the cluster stored in the estimate.
Non-NULL resamples clusters; pass NULL to force unit-level resampling.}

\item{...}{ignored.}
}
\description{
Each replication resamples units (or whole clusters when a cluster vector is
available), rebuilds the cohort structure from the resampled rows, and re-runs the
entire staggered estimator with \code{drop.infeasible = TRUE}, so the within-cohort
omega/lambda weights, any detrending, and the cohort weights are all re-fit inside
every draw.
}
