# results/ — derived analysis artifacts (committed)

Reduced outputs of the analysis scripts, read by the paper/supplement Rmds
for fast, computation-free rendering. Regenerate with:

    Rscript scripts/00_run_all.R --from analysis

See `_manifest.csv` for the seed, git SHA, and build date behind each file.
